Changelog for mxLogger.

mxLogger 1.0.2 (2026-06-19)
===========================
- Селектор тэгов в гриде журнала: пагинация по 10 тэгов на страницу со
  стрелками-навигацией (‹ N / M ›) и полем поиска сверху. Решает проблему
  слишком длинного списка при большом числе источников/пакетов. Выбор тэгов
  и строка поиска сохраняются при листании страниц.
- Cache-busting для CSS/JS менеджера: к подключению добавляется ?v=<mtime>
  файла, чтобы новые версии не залипали в кэше браузера и в объединённом
  кэше менеджера (compress_js/compress_css). В сервис добавлен путь
  assets_path.

mxLogger 1.0.1 (2026-06-17)
===========================
- Очистка журнала теперь уважает ВСЕ активные фильтры грида. Раньше при очистке
  отбрасывались фильтры «Пользователь/сессия/IP» (ident) и «Поиск по тексту»
  (query) — удалялось больше, чем показано в гриде. Также не передавался
  tags_match, из-за чего очистка по тэгам шла в режиме any вместо all грида.
- Условия фильтрации вынесены в общий построитель mxLoggerLogFilters — единый
  источник правды для выборки (getlist) и очистки (clear), чтобы они не
  расходились.
- Если фильтры не заданы, при очистке выводится отдельное предупреждение об
  удалении всего журнала.

mxLogger 1.0.0 (2026-06-16)
===========================
- Фикс charset таблицы логов: при install/upgrade таблица modx_mxlogger_log
  приводится к utf8mb4 (ALTER ... CONVERT TO). На серверах, где таблица была
  создана в дефолтном latin1, запись кириллицы падала с «1366 Incorrect string
  value» (например через плагин ms2: «Корзина очищена»). Резолвер чинит и
  существующую таблицу при апгрейде. Безопасно по длине индексов: class
  varchar(190)=760 байт (< 767), tags — FULLTEXT.

mxLogger 1.0.0-rc2 (2026-06-15)
===============================
- Опция skip_classes у log(): классы-прокладки (фасады/обёртки над логгером)
  трактуются как внутренние при определении «Источника» — надёжнее жёсткого
  skip=N (не зависит от числа кадров обёртки). Поддерживается точное имя класса
  или префикс пространства имён (строка, заканчивающаяся на «\»).

mxLogger 1.0.0-rc (2026-06-15)
==============================
- Первый релиз.
- Сервис-класс mxLogger: log/debug/info/warning/error, скоуп процесса process().
- Мультитэги: одна запись может иметь несколько тэгов (lowercase, [a-z0-9]).
  Хранение — CSV-колонка tags с FULLTEXT-индексом; фильтр any/all, режим
  mxlogger.tag_filter_mode (auto/fulltext/like) с LIKE-fallback для коротких тэгов.
- Таблица mxlogger_log: tags, process_uid, level, message, context(JSON), class, function, file, line, trace(JSON), user_id, session_id, ip, createdon.
- Автозахват вызывающего кода (class/function/file/line) и стэка с параметрами (объекты сворачиваются в имя класса).
- Режимы захвата: off/caller/full/auto (auto: caller, full для warning/error).
- Whitelist-фильтры записи: по пользователю, группе, сессии, куке.
- Менеджерный интерфейс: грид с фильтрами по тэгу/уровню/процессу/дате/тексту, окно детали, очистка.
- Сниппет mxLogger для вызова из чанков/Fenom.
- События mxlOnBeforeLogSave (до записи — можно отменить/изменить поля) и
  mxlOnAfterLogSave (после записи — для уведомлений). Ошибки обработчиков
  перехватываются и не ломают логирование.
- standalone.php — просмотр логов в обход MODX (CLI всегда; web по ключу), на
  случай, если приложение MODX не грузится. Читает БД напрямую через PDO.
- Плагин mxLoggerMiniShop2 (статичный, код из файла): логирование действий с
  корзиной (тэг cart) и оформления заказа (тэг order); сквозной тэг purchase;
  события одной воронки объединяются process_uid по сессии. Логируются только
  значимые действия (info/error), без debug. Требует установленного miniShop2.
- Авто-определение «Источника»: движок пропускает диспетчерские кадры фреймворка
  (*::invokeEvent, mod*::process, include/eval) и указывает реальный метод —
  напр. msCartHandler::add вместо бесполезного ::include.
